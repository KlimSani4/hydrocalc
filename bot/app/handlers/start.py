"""
Обработчик команды /start.
"""
from aiogram import Router
from aiogram.filters import CommandStart
from aiogram.types import Message

router = Router()


@router.message(CommandStart())
async def cmd_start(message: Message) -> None:
    """
    Обрабатывает команду /start.

    Отправляет приветственное сообщение с описанием возможностей бота.
    """
    welcome_text = (
        "👋 <b>Привет!</b>\n\n"
        "Я бот <b>HydroCalc</b> — помогу рассчитать дневную норму потребления воды "
        "для школьных групп и персонала.\n\n"
        "📊 <b>Что я умею:</b>\n"
        "• Рассчитывать норму воды с учётом возраста\n"
        "• Учитывать сезон (холодный/тёплый)\n"
        "• Корректировать расчёт по типу активности\n\n"
        "🔹 /calculate — начать расчёт\n"
        "🔹 /help — справка по использованию"
    )

    await message.answer(welcome_text, parse_mode="HTML")
