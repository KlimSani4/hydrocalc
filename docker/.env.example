# HydroCalc Docker Production - Переменные окружения
# Скопируй этот файл в .env перед запуском docker-compose.prod.yml
#
# Использование:
# cp .env.example .env
# nano .env  # заполни значения
# docker compose -f docker-compose.prod.yml up -d

# === PostgreSQL ===
# Пароль для базы данных (сгенерируй: openssl rand -base64 32)
POSTGRES_PASSWORD=change_me_in_production

# === Backend ===
# Секретный ключ для JWT (сгенерируй: openssl rand -hex 32)
SECRET_KEY=change_me_in_production

# === Redis ===
# Пароль для Redis (сгенерируй: openssl rand -base64 32)
REDIS_PASSWORD=redis_secret_change_me

# === Мониторинг (опционально) ===
# Sentry DSN
SENTRY_DSN=
