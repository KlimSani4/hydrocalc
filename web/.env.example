# HydroCalc Web - Переменные окружения
# Скопируй этот файл в .env и заполни значения
#
# ВАЖНО: Все переменные для Vite должны начинаться с VITE_
# иначе они не будут доступны в браузере

# === API ===
# URL backend API
# Локальная разработка (через прокси vite.config.ts):
VITE_API_URL=/api/v1

# Production (прямой URL на Render):
# VITE_API_URL=https://hydrocalc-api.onrender.com/api/v1

# === Аналитика (опционально) ===
# ID счётчика Яндекс Метрики
VITE_METRIKA_ID=
