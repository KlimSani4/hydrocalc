# HydroCalc Backend - Переменные окружения
# Скопируй этот файл в .env и заполни значения

# === База данных ===
# Локальная разработка:
DATABASE_URL=postgresql://hydrocalc:hydrocalc_dev@localhost:5432/hydrocalc

# Production (Render автоматически подставит):
# DATABASE_URL=postgresql://user:pass@host:5432/dbname

# === Безопасность ===
# Секретный ключ для JWT токенов (сгенерируй: openssl rand -hex 32)
SECRET_KEY=your-secret-key-change-in-production

# === Мониторинг ===
# Sentry DSN для отслеживания ошибок (опционально)
SENTRY_DSN=

# === Окружение ===
# development | staging | production
ENVIRONMENT=development

# === Redis (опционально, для кэширования) ===
# REDIS_URL=redis://localhost:6379/0
