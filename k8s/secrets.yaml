# Секреты для HydroCalc
# ВАЖНО: Реальные значения хранятся в отдельном manifests репозитории
# Этот файл - шаблон для локальной разработки
apiVersion: v1
kind: Secret
metadata:
  name: hydrocalc-secrets
  namespace: dev-hydrocalc
type: Opaque
stringData:
  POSTGRES_PASSWORD: "changeme"
  DATABASE_URL: "postgresql://hydrocalc:changeme@postgres:5432/hydrocalc"
  SECRET_KEY: "changeme"
  REDIS_PASSWORD: "changeme"
  SENTRY_DSN: ""
  BOT_TOKEN: ""
